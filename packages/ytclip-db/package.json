{
  "name": "@ytclip/database",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "migrate": "prisma migrate dev && prisma generate",
    "migrate:prod": "prisma migrate deploy",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "5.6.0",
    "prisma": "^5.6.0"
  }
}